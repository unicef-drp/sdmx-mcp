[project]
name = "sdmx-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.11"
dependencies = [
    "cachetools>=7.1.1",
    "httpx>=0.28.1",
    "fastmcp>=3.2.4",
    "mcp[cli]>=1.27.1",
    "python-dotenv>=1.2.2",
]

[tool.setuptools]
py-modules = ["server", "main"]
